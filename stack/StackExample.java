/*import java.util.Stack;

public class StackExample {
    public static void main(String[] args) {
        // Create a Stack instance
        Stack<Integer> stack = new Stack<>();

        // Push elements onto the stack
        stack.push(10);
        stack.push(20);
        stack.push(30);
        System.out.println("Stack after push operations: " + stack);

        // Peek the top element
        System.out.println("Top element (peek): " + stack.peek());

        // Pop elements from the stack
        System.out.println("Popped element: " + stack.pop());
        System.out.println("Stack after pop operation: " + stack);

        // Check if the stack is empty
        System.out.println("Is the stack empty? " + stack.isEmpty());

        // Search for an element in the stack
        int searchElement = 20;
        int position = stack.search(searchElement);
        if (position != -1) {
            System.out.println(searchElement + " is found at position (from top): " + position);
        } else {
            System.out.println(searchElement + " is not in the stack.");
        }

        // Iterate over the stack
        System.out.println("Iterating over the stack:");
        for (Integer element : stack) {
            System.out.println(element);
        }
    }
}
    */
